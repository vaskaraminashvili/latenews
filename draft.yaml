models:
  Category:
    title: json
    slug: string
    description: json nullable
    status: boolean
    deleted_at: timestamp

  Tag:
    title: json
    slug: string
    status: boolean
    deleted_at: timestamp
